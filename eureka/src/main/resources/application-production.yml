spring:
  application:
    name: eureka-service
  security:
    user:
      name: admin-eureka
      password: admin

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

logging:
  level:
    com.netflix.eureka: INFO
    com.cardgenerator: INFO 